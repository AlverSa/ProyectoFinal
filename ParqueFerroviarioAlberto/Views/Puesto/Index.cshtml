@model IEnumerable<ParqueFerroviarioAlberto.Models.Puesto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreDePuesto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreDePuesto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idPuesto }) |
            @Html.ActionLink("Details", "Details", new { id=item.idPuesto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idPuesto })
        </td>
    </tr>
}

</table>

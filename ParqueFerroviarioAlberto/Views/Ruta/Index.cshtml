@model IEnumerable<ParqueFerroviarioAlberto.Models.Ruta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.rutaTren)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idCiudad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.rutaTren)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idCiudad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idRuta }) |
            @Html.ActionLink("Details", "Details", new { id=item.idRuta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idRuta })
        </td>
    </tr>
}

</table>

@model IEnumerable<ParqueFerroviarioAlberto.Models.TrenEmpleado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idTren)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idEmpleado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idTren)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idEmpleado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idTrenEmpleado }) |
            @Html.ActionLink("Details", "Details", new { id=item.idTrenEmpleado }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idTrenEmpleado })
        </td>
    </tr>
}

</table>
